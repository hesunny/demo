# this is a yaml file

testServer:
    url: http://pm-debug2.dzsh.net

user:
    login_name: 134765280**
    login_password: *******

log:
    fileName: test.log
    backupCount: 5
    # 日志输出级别
    consoleLevel: WARNING
    fileLevel: DEBUG
    # 日志输出格式
    pattern: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'

